<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC
	"-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="PetDAO">
	<!-- 추가 설정 -->
	<!-- 기본 설정 -->

	<!-- 기본 설정 -->
	<insert id="insert">
		<![CDATA[
		insert into pets
		(seq, name, masterId, birthDate)
		values
		((select nvl(max(seq), 0)+1 from pets), #{name}, #{masterId}, #{birthDate})
		]]>
	</insert>
	<select id="selectList" resultType="pet">
		<![CDATA[
		select *
		from pets
		where masterId=#{masterId, jdbcType=VARCHAR}
		order by seq desc
		]]>
	</select>
	<select id="selectOne" resultType="pet">
		<![CDATA[
		select *
		from pets
		where seq=#{seq}
		]]>
	</select>
	<update id="update">
		<![CDATA[
		update pets
		set name=#{name}, birthDate=#{birthDate}
		where seq=#{seq}
		]]>
	</update>
	<delete id="delete">
		<![CDATA[
		delete
		from pets
		where seq=#{seq}
		]]>
	</delete>
</mapper>